//! Native Rust Proton launch semantics (Phase 2 item 2 of the valve-stack
//! directive). See `proton_abi.rs` for the port of Valve's `proton` script
//! logic; the runner trait lives in `crate::infra::runners::trait`.

pub mod proton_abi;
